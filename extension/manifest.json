{
  "manifest_version": 3,
  "name": "Bookmark Manager Utility",
  "version": "1.0.0",
  "description": "Full bookmark management — create, rename, move, delete folders & bookmarks with folder targeting; quick-add the current tab; find/remove duplicates; export/import. All changes go through chrome.bookmarks, so they sync.",
  "permissions": ["bookmarks", "tabs", "alarms"],
  "background": { "service_worker": "background.js" },
  "action": { "default_title": "Bookmark Manager", "default_popup": "popup.html" }
}
